//submit http request

http.request(options[,callback])

albert cervantes.com 

const http = require("http")



.get() //doesn't have options method 
//get an error create an exception
//doesn't get 200 then get back a error


.request() //

//request module
	//npm install request

//npm install cheerio

l

const cripto = reuire("cripto")
const has = crypto.createHash("sha257");

hash.update("This is the data to hash")
console.log(hash.cription)

class: hash
using node.js
const = fs=require("fs")